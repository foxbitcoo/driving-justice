---
name: driving-justice
description: >-
  从行车记录仪、手机拍摄的视频或图片中整理"轻量级、无需交警到场"的交通违法证据（远光灯炫目、闯红灯、逆行、加塞、违停、压实线、压水花溅人、不礼让斑马线、开车打手机、占用应急车道、货车违反车道通行规定等），核实车牌后预填中国大陆当地（如12345、交警专项举报渠道）的举报表单，违法行为定性依据现行《道路交通安全法》并现场核验官方条款原文，确保每条举报都能追溯到具体法条和具体证据。
  一段素材中可能存在多个可举报的违法瞬间，先列出候选让用户确认倾向再继续。
  本 Skill 依赖图像输入能力；处理视频时还需要原生视频理解能力，或能够在本地抽帧后把帧图片输入多模态模型。开始分析前必须预检当前模型和会话能力，能力缺失或无法确认时提醒用户切换模型并停止。
  车牌只有在前牌或后牌至少一面全部清晰，且该车牌画面能通过连续行车轨迹对应到违法瞬间时才提取；任何不确定都停止而不是猜测，杜绝生成式修复车牌。
  验证码、实名认证、最终提交等环节始终由用户手动完成。官方举报表单预填完成后，先用脚本把记录（含嵌入的证据图片）追加写入本地 Excel 台账；用户手动提交并取得举报单号后，再更新同一条台账记录。
  仅当用户明确点名"行车正义"或"driving-justice"时才加载本 Skill；普通对话中只提到交通违法、行车记录仪、远光灯、12345或交警随手拍等内容时，不自动启动本工作流。
---

# 行车正义（Driving Justice）

把"视频取证 → 违法定性 → 车辆确认 → 渠道匹配 → 表单预填 → 本地留痕"自动化，同时在**人车识别、验证码、最终提交**三处保留人工闸门。本 Skill 只协助整理证据与预填表单，不构成法律意见；违法行为的最终认定以当地交管部门为准。

## 参考文件（按需读取）

- `references/model-capability.md` — 模型图像/视频能力预检、抽帧降级路径与阻断提醒，用于进入 Step 0 前，**必读**
- `references/low-cost-model-route.md` — 可选的本地抽帧 → Doubao Seed 2.0 Lite → DeepSeek V4 → 单个 Excel 候选结果链路，用于授权素材试水
- `references/traffic-law.md` — 常见违法行为与《道路交通安全法》条款对照表 + 现场核验官方原文的方法，用于第 3、7 步
- `references/plate-verification.md` — 车牌确认的硬性标准，用于第 4 步，**必读**
- `references/channel-lookup.md` — 如何检索当地官方举报渠道，用于第 6 步
- `templates/statement-template.md` — 客观陈述写作规范，用于第 7 步
- `templates/ledger-schema.md` — 本地台账的字段结构与 `scripts/append_ledger.py` 用法，用于第 9 步

## 运行前硬闸门：模型多模态能力预检

在读取或分析用户素材前，先完整执行 `references/model-capability.md`。图片任务必须确认当前模型/会话具备图像输入能力；视频任务必须确认具备原生视频理解能力，或同时具备本地抽帧能力与图像输入能力。不要仅凭模型名称猜测能力。

能力已确认才进入 Step 0。能力缺失或能力未知时，按参考文件输出醒目的阻断提醒并停止，不得仅查看文件名、时长或元数据后假装已经理解画面。会话中途切换模型后必须重新检查。

## 可选：低成本实跑链路

用户明确要求对本人拍摄或已获授权的素材先做批量试水时，按 `references/low-cost-model-route.md` 运行。该路径的输入是本地视频或图片，输出是一个嵌入候选证据图片的本地 `analysis.xlsx`。

完整素材先按时间顺序扫描，再对每个候选窗口密集抽取原始帧复核。试跑 Excel 中的车牌由视觉模型判断，但只有结构化结果同时满足“前牌或后牌一面全部清晰、轨迹连续、车牌帧与行为帧引用有效”，且第二次不带候选答案的独立重读逐字一致时，确定性校验器才允许写入；否则留空。该结果仍是模型候选，不是官方认定。若要正式举报，必须返回 Step 2—9，并在 Step 4 由用户最终确认车牌与车辆对应关系。

## 工作流

### Step 0：前置检查

确认用户提供了至少一段视频或若干张图片。只有文字描述、没有影像素材时，直接告知"需要视频或图片证据才能继续"，不要凭描述编造车牌或情节。

### Step 1：扫描素材，列出候选违法事件

完整看一遍视频/图片，列出所有疑似违法的时刻，每条包含：时间点、行为的初步描述、涉及车辆的粗略外观（颜色、车型大类）。**不要**自动挑"最明显"或"最严重"的一个来处理——把完整清单呈现给用户，即使某些候选后续判断不构成违法。

### Step 2：与用户确认举报目标

让用户从候选清单里选定要举报的一个或多个目标。只有用户明确选定后才继续往下走，不要替用户做这个决定。

### Step 3：违法行为定性

对照 `references/traffic-law.md`，把用户选定的行为对应到具体条款号。表中的条款号可以直接用于初筛，但**条款原文不能凭记忆写**——生成最终陈述前（Step 7）必须按该文件"现场核验官方原文"一节的方法，去官方法律法规数据库核对现行原文并留存链接。表中没有覆盖、或行为存在争议时，如实告知"具体条款需要人工核实"，绝不编造条款编号或处罚标准。

### Step 4：车牌与车辆信息确认（硬闸门）

严格执行 `references/plate-verification.md` 的标准：前牌或后牌至少一面全部清晰，且该车牌帧与违法行为帧之间的车辆轨迹连续，能确认是同一辆车。只拍到一面车牌本身不是停止理由；若可见车牌字符或车辆对应关系有歧义则停止，不写入任何猜测值，并询问用户是否有更清晰的素材。通过初步验证后，把可见一面的车牌截图连同原始帧路径交给用户确认无误，才算完成这一步。

### Step 5：提取证据关键帧

针对确认通过的目标，选出能证明违法行为的几张关键帧（行为发生瞬间、前牌或后牌中可见一面的清晰帧、连接二者的连续轨迹帧等），记录每帧对应的时间点。若前后两面都拍到可以一并保留，但不是通过条件。展示副本可以裁剪、放大、增强对比度，但**不得使用生成式方式修复或补全画面内容**，且必须保留原始帧文件不做修改。

### Step 6：定位举报渠道

到这一步才需要地点信息。按 `references/channel-lookup.md` 的方法：默认直接问用户"这发生在哪个城市"，不要靠画面线索自行猜测城市；再按该文件的优先级，先查该违法类型是否有专项官方渠道，没有再退到当地"12345"等通用渠道。

### Step 7：生成客观举报陈述

先按 `references/traffic-law.md` 的"现场核验官方原文"方法，核对本次要引用的条款现行原文和官方链接。再按 `templates/statement-template.md` 的结构写陈述文字，只描述视频能证明的事实，附上法律依据（法条+原文+链接）和证据说明（文件名+时间段+关键帧路径），不加主观评价或情绪化用词，写完交给用户确认。

### Step 8：预填表单

打开官方表单页面，按当地实际字段填入：地点、属地部门选择、违法行为描述、发生时间、车牌等。默认选择"不公开""隐私保护"一类的选项（如表单提供）。

以下内容**永远不代劳**，遇到就停下并明确告知用户去手动完成：验证码、二维码扫描、姓名、身份证号、手机验证码、任何"承诺真实性/授权"类勾选框、最终提交按钮。

### Step 9：更新本地台账

这里的“本地台账”是用户自己保存和查看的 `ledger.xlsx`，不是 Step 8 中的 12345、交警专项渠道等官方举报表单。

官方举报表单预填完成、用户尚未点击最终提交时，按 `templates/ledger-schema.md` 的字段结构，把本次举报的信息（含证据关键帧图片路径）整理成一份 JSON，运行 `scripts/append_ledger.py` 追加写入本地 `ledger.xlsx`（不存在则自动创建带表头），将“举报状态”记为“已预填待用户提交”。

用户手动提交官方表单并取得举报单号后，运行 `scripts/update_ledger.py`，按台账“序号”找到刚才的同一条记录，补充举报单号并把状态更新为“已提交待处理”。更新时不得新增第二条记录，也不得改动车牌、违法事实或证据图片等已确认内容。

本地 Excel 的新增和更新都必须用脚本执行，不要让模型手工拼接 Excel 内容。

## 硬性红线（不可协商）

- 车牌不清楚就是不举报，没有"大概率对"这种中间状态
- 不用生成式方式"修复""补全"车牌或车辆细节，只能裁剪/放大/增强展示，原图必须保留
- 验证码、实名认证、最终提交永远由用户自己操作
- 陈述只写视频能证明的事实，不做主观定性或情绪化描述，法律依据必须现场核验原文，不能凭记忆编条款
- 一段素材有多个候选目标时，必须让用户选，不能替用户决定举报谁
- 城市/地点默认直接问用户确认，不靠画面自行猜测（除非画面明确出现城市名称）
